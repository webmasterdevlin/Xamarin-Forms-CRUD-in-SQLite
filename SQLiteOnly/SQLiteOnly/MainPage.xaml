<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SQLiteOnly"
             x:Class="SQLiteOnly.MainPage">
    <StackLayout>
        <StackLayout Orientation="Horizontal">
            <Button Text="Add" BackgroundColor="DodgerBlue" TextColor="{OnPlatform iOS=White}" Padding="20, 10" HorizontalOptions="StartAndExpand" Clicked="OnAdd" />
            <Button Text="Update" HorizontalOptions="CenterAndExpand" Clicked="OnUpdate"  />
            <Button Text="Delete" BackgroundColor="Crimson"  TextColor="{OnPlatform iOS=#fff}" Padding="20, 10" HorizontalOptions="EndAndExpand" Clicked="OnDelete" />
        </StackLayout>
        <ListView x:Name="MyListView" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>